TODO:

+ convert table to boostrap 4
+ on backgroundjs: 
  + gather and store categories and their ids
  + check port for recieving json of transactions
+ on content.js
  + populate categories selection
  + gather all row info into a json
+ on endpoints.js:
  + function to POST transactions
+ Clean up code
